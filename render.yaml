version: "1"
services:
  - type: web
    name: flask-chat-hf
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: HF_TOKEN
        sync: false
    plan: free
